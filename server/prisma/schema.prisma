generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Event {
  Id              Int     @id @default(autoincrement())
  Name            String
  Venue           String
  Date            Int
  TicketPrice     Decimal
  ContractAddress String
}
